spring.application.name=MediSlot-bakend

spring.datasource.url=jdbc:postgresql://localhost:5432/medi_slot_database
spring.datasource.username=postgres
spring.datasource.password=Deep@k412

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT secret + expiry (we?ll use later)
jwt.secret=YourSuperSecretKey123
jwt.expirationMs=3600000